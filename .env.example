# Copy to .env and fill in. NEVER commit the real .env file.
# Supabase (Phase 1+)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Gemini (Phase 2+) — used by the extraction + assistant features
VITE_GEMINI_API_KEY=
